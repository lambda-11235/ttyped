
# The identity function of types of type *, as well as its type.
id = (\*. (\0. 0))
idT = (@*. (@0. 1))

const = (\*. (\0. (\*. (\0. 2))))

pair = (\*. (\*. (\1. (\1. (\*. (\(@4. (@4. 2)). ((0 3) 2)))))))
pairT = (\*. (\*. (@*. (@(@2. (@2. 2)). 1))))


botType = (@*. 0)

unitType = idT
unit = id


sigmaType = (\*. (\(@0. *). (\1. (\(1 0). (@*. (@(@4. (@(4 0). 2)). 1))))))

sigma = (\*. (\(@0. *). (\1. (\(1 0).
  (\*. (\(@4. (@(4 0). 2)). ((0 3) 2)))))))
